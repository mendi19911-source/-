# מסמך אפיון — בוט AI תפעולי לרשת חנויות סלולר
## גרסה 1.0 | יוני 2026

---

## 1. רקע והגדרת הצורך

רשת החנויות מפעילה עשרות נקודות מכירה. כל חנות עובדת מול מערכת CRM מרכזית לניהול עסקאות ניוד, הפעלת קווים, ושינויי חבילות. כיום, כל בקשת מידע או טיפול בבעיה מחייבת פנייה ידנית של נציג החנות לצוות תפעול מרכזי דרך ערוץ מסרים — דבר הגורם לעיכובים, לעומס על הצוות המרכזי, ולחוויית שירות ירודה עבור נציגי החנויות.

הפתרון המוצע הוא מערכת בוט AI תפעולי שתשמש כממשק שיחתי בין נציגי החנויות לבין מערכות המידע הקיימות — CRM, מאגר חבילות, ומאגר זיהוי מפעילים. הבוט יטפל בשכבת השאלות והבקשות השגרתיות, ויעביר פניות מורכבות לנציגים אנושיים בצורה חכמה ומובנית.

---

## 2. מטרות המערכת

1. מתן מענה מיידי לנציגי חנויות על שאלות תפעוליות שוטפות
2. הפחתת העומס על צוות התפעול המרכזי
3. שיפור זמן התגובה לנציגים מדקות לשניות
4. תיעוד אוטומטי של פניות ב-CRM
5. זמינות מלאה 24/7 כולל שעות שמחוץ לשעות העבודה
6. פלטפורמה מדרגית להרחבה עתידית — וואטסאפ, קול, תמונות

---

## 3. הגדרת משתמשים

### 3.1 משתמש ראשי — נציג החנות
- עובד בנקודת מכירה של הרשת
- פונה לבוט לקבלת מידע תפעולי בזמן אמת
- שולח הודעות קצרות ולא תמיד מסודרות
- לעיתים שולח בקשות מרובות באותה שיחה
- פועל לעיתים תחת לחץ כאשר לקוח עומד מולו

### 3.2 משתמש משני — נציג תפעול מרכזי
- מקבל העברות מהבוט לפניות שדורשות טיפול אנושי
- מקבל סיכום שיחה אוטומטי עם כל העברה
- נדרש לדעת מה כבר בדק הבוט לפני שהפניה הגיעה אליו

---

## 4. ארכיטקטורת המערכת

### 4.1 תרשים רכיבים

```
[נציג חנות]
     |
[ממשק שיחה — Web Chat / WhatsApp]
     |
[שרת Backend — Python + FastAPI]
     |          |              |
[CRM API]  [Claude AI]  [Provider API]
```

### 4.2 שרת Backend

- שפה: Python 3.11 ומעלה
- Framework: FastAPI
- שרת: Uvicorn עם Nginx כ-reverse proxy
- פריסה: Linux server, systemd service
- ניהול קונפיגורציה: קובץ .env לכל הקרדנציאלים

### 4.3 מנוע AI

- ספק: Anthropic
- מודל: Claude Haiku — מהיר, חסכוני, תומך עברית ברמה גבוהה
- גישה: Anthropic Python SDK
- היסטוריית שיחה: 10 הודעות אחרונות מועברות עם כל בקשה מצד הלקוח
- System Prompt: דינמי — נבנה בכל בקשה עם נתוני CRM עדכניים

### 4.4 ממשק משתמש

שלב 1: Web Chat — ממשק HTML/CSS/JS בסגנון וואטסאפ, RTL עברית, מוגש מהשרת
שלב 3: WhatsApp Business API — ממשק ישיר דרך Meta Cloud API

---

## 5. זרימת נתונים — בקשה טיפוסית

```
1. נציג שולח הודעה + מספר טלפון החנות
2. Backend מאמת את מספר החנות מול CRM (checkUser)
3. Backend שולף עסקאות פתוחות של החנות
4. Backend שולף הערות פתוחות לעסקאות (getDealNotes)
5. Backend שולף רשימת חבילות עדכנית (get-packages)
6. זיהוי כוונה: האם יש מספר טלפון בהודעה ובקשת מפעיל?
   אם כן — קריאה ל-checkProvider לכל מספר
7. בניית System Prompt דינמי עם כל ההקשר
8. קריאה ל-Claude API עם הפרומפט והיסטוריית שיחה
9. זיהוי תגית [TRANSFER:חברה] בתשובה
   אם יש — הפעלת לוגיקת העברה
10. החזרת תשובה ל-Client
```

---

## 6. אינטגרציות CRM — Endpoints נדרשים

### 6.1 קיים

| Endpoint | פרמטרים | מה מחזיר |
|----------|----------|-----------|
| checkUser | phone | פרטי חנות + רשימת עסקאות פתוחות |
| checkProvider | phone | חברת המפעיל של המספר |
| get-packages | — | רשימת חבילות עם מחירים |

### 6.2 נדרש לפיתוח

| Endpoint | פרמטרים | מה מחזיר | עדיפות |
|----------|----------|-----------|--------|
| getDealNotes | deal_id | הערות פתוחות לעסקה | דחוף |
| addNote | deal_id, text, agent_name | אישור יצירת הערה | דחוף |
| transferToAgent | deal_id, company | אישור העברה + פרטי הנציג | דחוף |
| getAgentList | company | רשימת נציגים זמינים לפי חברה | בינוני |
| refreshLine | phone | אישור רענון קו | בינוני |
| getCustomerLines | id_number, company | קווים של לקוח בחברה | בינוני |
| checkSIMStatus | iccid | האם הסים פנוי / חסום / מוקצה | בינוני |
| getDealById | deal_id | פרטי עסקה ספציפית | עתידי |

### 6.3 פורמט תגובה סטנדרטי מצופה

כל endpoint יחזיר JSON במבנה הבא:

```json
{
  "success": true,
  "data": {},
  "error": "הודעת שגיאה אם success הוא false"
}
```

---

## 7. לוגיקת עסק — הגדרות התנהגות הבוט

### 7.1 זיהוי כוונה

| כוונה | זיהוי | פעולה |
|-------|-------|-------|
| בדיקת סטטוס עסקה | שם לקוח / מספר עסקה | שליפה מ-CRM |
| בדיקת מפעיל | מספר 05XXXXXXXX + כוונה מפורשת | קריאת checkProvider |
| שאלת חבילות | שאלה על מחיר או גיגה | שליפה מ-get-packages |
| בעיה טכנית | לא עובד, אין אינטרנט | הנחיה לפתרון + הצעת העברה |
| בקשת זירוז | דחוף, הלקוח איתי | תגובה מיידית + הצעת העברה |
| הקמת עסקה | להקים, עסקה חדשה | הפניה למערכת CRM |

### 7.2 טיפול מולטי-נושא

השיחה יכולה להכיל מספר נושאים מקבילים. הבוט מחויב לזהות כל נושא בנפרד ולא לערבב ביניהם.

דוגמה:
```
נציג: "מה הסטטוס של עסקת דוד לוי?"
בוט: [תשובה על הסטטוס]
נציג: "ואגב, 0521234567 באיזה חברה?"
בוט: [תשובה על המפעיל — נושא נפרד לחלוטין]
```

### 7.3 הגדרות סטטוסי עסקה

| קוד סטטוס | הסבר לנציג |
|-----------|------------|
| OPEN | פתוחה — טרם טופלה |
| PROCESS_SHOP | דורש טיפול מהחנות — הנציג צריך להיכנס לעסקה ולראות מה נדרש |
| WAITING_CONNECT | ממתין לחיבור |
| CONN_NOT_NIY | חובר — ממתין לאישור ניוד מהלקוח. ברגע שהלקוח יאשר העסקה תתקדם |
| NIYUD_ACTIVATED | הניוד יצא לדרך — הקו בתהליך ניוד, עד 20 דקות אמור לעבוד |
| DONE | הושלמה בהצלחה — הקו אמור לעבוד |
| CANCELLED | מבוטלת |
| ROBOT_ERROR_SYS | שגיאת מערכת — יש לפנות לתפעול |

### 7.4 לוגיקת העברה לנציג אנושי

הבוט מעביר לנציג אנושי בשני מקרים:

א. בקשה שחורגת מסמכות הבוט — פעולות כגון: הפעלת ניוד, החלפת SIM, עדכון אשראי, עקיפת ניוד, שחרור חסימה, רענון קו.

ב. מידע שאינו קיים בנתונים שהבוט קיבל.

זרימת העברה:
```
בוט: "אין לי את המידע הזה. האם תרצה שאעביר לנציג?"
נציג: "כן"
בוט: בודק אם החברה כבר ידועה מהשיחה
  אם כן — [TRANSFER:שם_חברה]
  אם לא — "לנציג של איזו חברה? סלקום / פרטנר / פלאפון / גולן / הוט / wecom"
נציג: בוחר חברה
בוט: [TRANSFER:שם_חברה] + שולח סיכום שיחה לנציג
```

כלל חובה: הבוט לעולם לא מפנה נציג לחברות חיצוניות. בכל מקרה — מעביר לנציג הפנימי שלנו.

### 7.5 תיעוד אוטומטי ב-CRM

עם כל פנייה שמסתיימת בהעברה לנציג, הבוט שולח קריאת addNote ל-CRM עם:
- שם החנות ומספרה
- תקציר הפנייה
- מה הבוט בדק וענה
- שעת הפנייה

---

## 8. ממשק משתמש — Web Chat

### 8.1 מבנה כללי

- Header: לוגו + שם הבוט
- שורת מספר חנות לאימות
- אזור הודעות: RTL, עברית, גלילה אוטומטית
- מחוון "הבוט מקליד..."
- תיבת טקסט + כפתור שליחה

### 8.2 סוגי בועות הודעה

| סוג | עיצוב | יישור |
|-----|-------|-------|
| הודעת נציג | ירוק בהיר | ימין |
| תשובת בוט | לבן עם צל | שמאל |
| העברה לנציג | צהוב, מסגרת בולטת | מרכז |

### 8.3 עיצוב ונגישות

- כיוון RTL מלא
- גופן Arial
- נייד-ראשון, max-width 540px
- Enter לשליחה, Shift+Enter לשורה חדשה
- תמיכה ב-Markdown בסיסי: bold ו-italic

---

## 9. אבטחה ופרטיות

### 9.1 ניהול קרדנציאלים

- כל מפתחות ה-API מאוחסנים בקובץ .env בשרת בלבד
- לא נשמר מידע רגיש בקוד המקור
- קובץ .env לא מועלה ל-Git בשום מקרה

### 9.2 אימות נציגים

- כל בקשה מאמתת את מספר הטלפון מול CRM (checkUser)
- מספר לא מזוהה — הבקשה נדחית
- אין גישה לנתונים של חנות אחרת

### 9.3 היסטוריית שיחה

- היסטוריית שיחה נשמרת בצד הלקוח בלבד, ב-session הדפדפן
- לא נשמר מידע על שיחות בשרת
- סגירת הדפדפן מוחקת את ההיסטוריה

---

## 10. ביצועים ומדרגיות

| פרמטר | יעד |
|--------|-----|
| זמן תגובה | פחות מ-3 שניות |
| עומס בו-זמני | עד 50 נציגים מקבילים |
| זמינות | 99.5% uptime |
| גיבוי | restart אוטומטי דרך systemd |

---

## 11. לוח זמנים

### שלב 1 — תשתית ויכולות בסיס
- הקמת סביבת Python ו-FastAPI
- חיבור CRM: checkUser, checkProvider, get-packages
- חיבור Claude API
- ממשק Web Chat
- העלאה לשרת ייצור
- בדיקות קצה-לקצה

### שלב 1.5 — השלמות ו-API חדש
- חיבור getDealNotes
- חיבור addNote
- חיבור transferToAgent
- בדיקות עם חנויות פיילוט

### שלב 2 — יכולות מורחבות
- Vision API — קריאת תמונות
- Whisper API — הקלטות קוליות
- תמיכה בערבית
- התראות פרואקטיביות

### שלב 3 — WhatsApp
- חיבור Meta Cloud API
- Webhook handler
- תמיכה בכל סוגי המדיה
- פריסה לכל החנויות

---

## 12. תרחישי בדיקה

### תרחיש 1 — סטטוס עסקה
קלט: "שלום, מה הסטטוס של דוד לוי?"
ציפייה: שליפת עסקה, הצגת סטטוס בעברית פשוטה

### תרחיש 2 — בדיקת מפעיל
קלט: "0521234567 באיזה חברה?"
ציפייה: קריאת checkProvider, תשובה עם שם החברה

### תרחיש 3 — מספרים מרובים
קלט: "בדוק 0521234567 ו-0534567890"
ציפייה: בדיקה לכל מספר בנפרד, תשובה מסודרת

### תרחיש 4 — העברה לנציג
קלט: "צריך לעקוף ניוד"
ציפייה: "אין לי הרשאה לכך. האם תרצה שאעביר לנציג?"

### תרחיש 5 — מולטי-נושא
קלט 1: "מה הסטטוס של מרים כהן?"
קלט 2: "ואגב, 0512345678 באיזה חברה?"
ציפייה: תשובות נפרדות לכל שאלה, ללא ערבוב

### תרחיש 6 — חנות לא מזוהה
קלט: מספר טלפון שאינו רשום ב-CRM
ציפייה: "המספר לא מזוהה במערכת"

---

---

## נספח א — שלב עתידי (ממתין לאישור API)

הסעיפים הבאים מתוכננים אך **לא ניתן לחבר כרגע** — יופעלו כשה-API יהיה זמין.

### א.1 יכולות מורחבות (שלב 2)

**קריאת תמונות (Vision API)**
- צילום ת"ז → חילוץ שם ומספר ת"ז
- צילום הודעת שגיאה → זיהוי הבעיה
- צילום גב סים → קריאת ICCID
- מימוש: Claude Vision API

**הודעות קוליות**
- נציג שולח הקלטה → תמלול → מענה
- זרימה: [אודיו] > [Whisper API] > [Claude] > [תשובה]

**תמיכה בערבית**
- זיהוי שפה אוטומטי, מענה בשפת הנציג

**התראות פרואקטיביות**
- עסקה תקועה מעל 3 שעות → התראה לנציג
- מימוש: Cron job כל 30 דקות

### א.2 WhatsApp Business API (שלב 3)

- חשבון Meta Business מאומת
- מספר WhatsApp Business ייעודי
- HTTPS + Webhook endpoint
- תמיכה בטקסט, תמונה, קול, PDF

---

## נספח ב — מבנה קבצי הפרויקט

```
allin-bot/
├── .env                    # קרדנציאלים — לא מועלה ל-Git
├── .env.example            # תבנית ריקה לסביבת פיתוח
├── .gitignore
├── requirements.txt
├── main.py                 # FastAPI app — נקודת כניסה
├── crm.py                  # קליינט CRM אסינכרוני
├── claude_client.py        # עטיפה ל-Anthropic SDK
├── system_prompt.py        # בניית פרומפט דינמי
├── intent.py               # זיהוי כוונה מהטקסט
├── transfer.py             # לוגיקת העברה לנציג
├── notes.py                # יצירת הערות ב-CRM
├── static/
│   └── index.html          # ממשק Web Chat
├── allin-bot.service       # systemd unit
└── README.md               # הוראות התקנה ופריסה
```
